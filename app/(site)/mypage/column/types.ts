export const PAGE_SIZE_OPTIONS = [10, 20, 50, 100];

export type MyColumnPostRow = {
  id: string;
  slug: string;
  number: number;
  section: string;
  category: string;
  title: string;
  createdAt: string;
};
